
// loads models in correct order

const userModel = require('../models/user.model')


const personModel = require('../models/person.model')
const ticketModel = require('./event.model')
const ticketPersonModel = require('./ticket.model')
